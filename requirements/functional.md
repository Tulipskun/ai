# ข้อกำหนดด้านการทำงาน

REQ-001 — Harness รับ canonical input ได้โดยไม่ขึ้นกับ transport

REQ-002 — CLI และ Discord สามารถใช้ Harness/Agent runtime เดียวกันได้

REQ-003 — แต่ละ session มี state แบบถาวรที่แยกจากกัน และสามารถกู้คืนได้หลัง process restart

REQ-004 — Agent สามารถทำลูป model → tool call → tool result → model จนกว่าจะไม่มี tool call เหลือ

REQ-005 — ความล้มเหลวของ tool ต้องถูกส่งกลับมาในรูปแบบ tool result เพื่อให้ Agent สามารถกู้คืนได้โดยไม่ทำให้ทั้ง turn ล้มเหลว

REQ-006 — รูปแบบ request/response เฉพาะของ provider ต้องถูกแยกไว้หลัง adapter และแปลงผ่าน canonical SDK model

REQ-007 — การค้นหา provider model ต้องใช้ catalogue ของ provider ที่ดึงจากระบบจริง และเมื่อ refresh สำเร็จต้องแทนที่ข้อมูล catalogue เดิมที่ค้างอยู่

REQ-008 — การ retry ต้องใช้ API key ของ session ที่เลือกไว้ และห้ามเปลี่ยน key โดยอัตโนมัติ

REQ-009 — output แบบ streaming จะต้องไม่ถูก replay โดยอัตโนมัติหลังจากเริ่มส่ง output แล้ว

REQ-010 — Browser automation ต้องใช้งานได้ผ่าน Go CDP implementation ที่อยู่ในตัว โดยไม่ใช้ Playwright หรือ Node.js worker รองรับ Firefox ESR 140 ผ่าน BiDi `/session` (TCP dial loop up to 15s, WS upgrade probe `ws://127.0.0.1:port/session` expecting 101, `session.new` id 1 with acceptInsecureCerts true; minimal BiDi dispatch covers open/navigate/snapshot/close, others return firefox-bidi-unsupported naming method; Chromium (Chrome/Chromium/Edge) launches with `--remote-debugging-pipe` over process stdio pipes with no loopback TCP listener and no DevToolsActivePort file dependency) นอกเหนือจาก Chrome/Chromium/Edge โดยไม่ใช้ geckodriver/Marionette Chromium เป็น default browser (`browser` default `chromium` ผ่าน `--remote-debugging-pipe`, Firefox ยังเลือกได้ผ่าน config, `auto` เลือก Chromium ก่อนเสมอ) และ headless default ยังคง `false`; daemon boot เตรียม browser client ไว้เท่านั้นโดยไม่ launch browser — browser จะ start แบบ lazy เมื่อมี browser tool call ครั้งแรก (`Call`/`ListPages`/`AttachPage` ผ่าน ensureStarted, managed mode `Start`, attach mode `Attach`) จึงไม่มี headed window โผล่จาก `ai update` เพียงอย่างเดียว; `stopDaemon`/daemon exit ต้องฆ่า tracked browser child PID (`browser.pid`, interrupt ก่อนแล้ว kill fallback ผ่าน `Close`/`killBrowserPID`/`killBrowserChild`) เพื่อไม่ให้ headed window ถูก orphan

REQ-011 — Runtime configuration ต้องเป็นแบบไฟล์ภายใต้ layout `~/.local/share/ai` ที่กำหนดไว้ และไม่ต้องใช้ environment variable ในการตั้งค่า — **ยกเลิก/แทนที่: entry.json เดินมีแค่ discord/cli; ตอนนี้ mobile เท่านั้น (CHANGE-059)**

REQ-012 — Project requirements ต้องเก็บอยู่ใน repository และถือเป็น source of truth สำหรับงานของโปรเจค

REQ-013 — ก่อน implement request ใหม่ AI ต้องตรวจสอบ repository requirements และบันทึก specification change ที่ request นั้นทำให้เกิดขึ้น

REQ-014 — เมื่อสร้าง software project ใหม่ AI ต้องสร้างและเติมข้อมูลใน `requirements/` ของ project นั้นก่อนเริ่ม implementation ในส่วนสำคัญ

REQ-015 — การเปลี่ยนแปลงโค้ดต้องรักษาความรับผิดชอบของแต่ละ module ให้ชัดเจน และต้องเพิ่มหรือแก้ functionality ใน module ที่รับผิดชอบโดยตรง เว้นแต่มีการเปลี่ยน architecture ที่บันทึกไว้อย่างชัดเจน

REQ-016 — เมื่อเปิด planning, Main Agent จะได้รับ planning tools, tools สำหรับ orchestration ของ sub-agent, tool สำหรับส่งต่อ opaque file reference และ read-only context tools (read_file/read_files/list_directory/search_files) สำหรับอ่านโค้ดและบริบทด้วยตนเอง (CHANGE-054) (ชื่อ/ชนิด/ขนาด/relative path ภายใน file store) เท่านั้น ไม่ใช่เนื้อหาไฟล์ และต้องปฏิเสธ write/exec call โดยตรง (write_file/edit_file/bash/run_job/browser/attachment-write) รวมถึงหลังจากบันทึกแผนแล้วด้วย ผลลัพธ์ของ orchestration tools ยังคงเป็น evidence-only — Main Agent ห้ามได้รับ byte stream, base64, MIME data หรือเนื้อหาไฟล์ผ่าน tool result ของ orchestration (ข้อยกเว้น CHANGE-054: Main อ่านไฟล์เองได้โดยตรงผ่าน read tools) file reference ต้องถูก resolve โดย worker agent หรือ transport module เท่านั้น Main Agent ตรวจสอบบริบทด้วยตนเองผ่าน read tools (อ่าน index.md ก่อน แล้ว batch read_files) เป็นอันดับแรก แล้วเขียน delegation contract สั่ง worker (CHANGE-054) โดยความพยายามต้องได้สัดส่วนกับความซับซ้อนของงาน: งานที่ต้องใช้ repository context ให้ Main อ่านเองใน main context ก่อน — งานคิด/ออกแบบ/ตัดสินใจทำใน main context เสมอ (serial on thinking) delegate เฉพาะงาน execute ที่มีขอบเขตชัด หรืองาน lookup แบบ read-only ที่ขนานกันได้ (CHANGE-054) งานเล็กน้อยที่ไม่ต้องใช้ repository context (เช่น สร้าง/ลบไฟล์เดียว) ต้องใช้แผนขั้นเดียวที่สั้นที่สุดโดยข้าม investigation แยก และแผนทุกขนาดต้องมีจำนวน step น้อยที่สุดที่ครอบคลุมเป้าหมาย และทุก turn ของ Main Agent เมื่อมีแผนอยู่แล้ว system prompt ต้องแนบ checklist ปัจจุบัน (ลำดับ step พร้อมสถานะ pending/ready/running/awaiting_review/failed/completed) เพื่อให้ Main Agent เป็นผู้ถือภาพรวมและตรวจรายการด้วยตนเอง (CHANGE-022) ข้อความที่ไม่ใช่งาน (ทักทาย/ขอบคุณ/acknowledgement/คำถามที่ตอบได้ตรงจากบทสนทนาโดยไม่ใช้ tools, repository context หรืองานที่ต้องทำ) ให้ Main Agent ตอบตรงทันทีโดยไม่สร้างแผน ไม่ delegate และไม่ investigate (CHANGE-032)

REQ-017 — เมื่อ `DisablePlanning: true`, execution/worker agent ต้องคง system prompt และ execution tool definitions ที่ได้รับมาไว้ทั้งใน turn ปกติและ streaming turn รวมถึงตอนทำต่อหลัง tool result; Harness ห้ามแทรกข้อจำกัดของ Main Agent หรือ planning/delegation tools เข้าไป ค่าเริ่มต้นของคำสั่ง sub-agent ต้องจำกัดงานให้อยู่ใน scope ที่ได้รับ ห้าม delegation ต่อ และห้ามสื่อสารกับ end user โดยตรง และต้องรายงานสิ่งที่ตรวจพบ/ผลลัพธ์ให้ planner การตรวจสอบผลต้องใช้วิธีที่น้อยที่สุดแต่เพียงพอ: คำสั่งเดียวที่พิสูจน์ผลลัพธ์ได้ (หรือ shell line เดียวที่รวมการตรวจที่เกี่ยวข้อง) ห้ามทำซ้ำรายการที่เทียบเท่ากัน (เช่น ls/wc/cat/stat ไฟล์เดียวกัน) เมื่อพิสูจน์ผลได้แล้ว และห้ามลองสูตรคำสั่งแบบอื่นต่อหลังจากการตรวจสำเร็จแล้ว ภาษาที่ใช้ระหว่าง planner↔worker (ทั้ง task ที่สั่งและรายงานที่ส่งกลับ) ต้องเป็นภาษาอังกฤษเสมอเพื่อลด token ไม่ว่าจะพูดภาษาใดกับผู้ใช้ (CHANGE-024)

REQ-018 — การประกอบ role prompt ต้องรักษา repository requirements และ custom context ไว้ ไม่ตัด section หรือบรรทัดที่ไม่เกี่ยวข้องออกด้วยการจับคู่ชื่อ tool แบบ heuristic ขอบเขต role ของ Main Agent ต้องมีผลเหนือคำสั่ง direct-execution ที่ขัดแย้งกันใน context ที่ได้รับมา และค่าเริ่มต้นของ CLI/SDK ที่สร้างในตัวต้องไม่ขัดแย้งกับ role ที่ได้รับมอบหมาย

 REQ-019 — (แก้ไขด้วย CHANGE-025, CHANGE-034) `delegate_to_subagent`, `follow_up_subagent`, `continue_subagent` ต้องคืน control ให้ Main Agent ทันทีพร้อม job id เพื่อ Main ไม่ค้างและรับข้อความใหม่ได้ ระหว่าง worker รัน orchestration ต้องส่ง progress report เข้า Main เป็น continuation turn แบบ hybrid milestone บวก anomaly-gated (milestone tools: write_file/edit_file/bash, anomaly: error สองครั้งติด, backstop: ครบ X tool calls นับจาก report ล่าสุด โดย X จาก config `sub_agent.report_every_tool_calls` ค่าเริ่มต้น 20 และจำกัดไม่เกิน MaxMidJobReports ครั้งต่อ job ค่าเริ่มต้น 3; report เป็น delta เฉพาะ tools ใหม่นับจาก report ล่าสุดพร้อม 1-line status header และตัด result excerpt ที่ 300 runes) ให้ Main ตรวจ scope ว่าขาดหรือเกิน — ถ้าเกิน/ออกนอกขอบเขตให้ `stop_subagent` (blocking) แล้ว `follow_up_subagent` งานใหม่ใน session เดิม; ถ้าตรง scope ให้ตอบสั้นแล้วรอต่อ เมื่อ job ถึง terminal state ต้องส่ง final handoff report เป็น continuation turn เช่นเดิม: `delegate_to_subagent`, `follow_up_subagent`, `continue_subagent` ต้องรอจน job ถึง terminal state แล้วส่งรายงานครบถ้วนกลับเป็น tool result เดียว (สถานะ, step, worker session, ประวัติ tool ทุกชิ้นพร้อม arguments/รายละเอียดและผลลัพธ์รวม error flag, ข้อความสรุปสุดท้าย) โดยค่าของแต่ละ tool result ในรายงานต้องถูกจำกัดความยาวเพื่อไม่ให้ history บวม — Main Agent ห้ามต้องเรียก tool ใดเพื่ออ่านผลซ้ำ และไม่มี tool สำหรับ polling status/history อีก ผลลัพธ์ต้องถูกส่งให้ Main Agent ตรวจสอบเท่านั้น ห้ามรับหรือเลื่อนแผนต่อโดยอัตโนมัติ Main Agent ต้องอ่านรายงานที่ส่งมาในมือ ตรวจสอบความสำเร็จ และยอมรับ step อย่างชัดเจนผ่าน `accept_subagent_result` พร้อม verification evidence การ review จำกัดอยู่ที่สรุป, ประวัติ tool, validation evidence เท่านั้น ไม่นับ raw file content หรือ large binary payload ที่แนบมากับ report (Main อาจ spot-check ด้วยการอ่านไฟล์เองผ่าน read tools แต่การ accept ยังต้องมี verification evidence ทุกครั้ง) (CHANGE-054) ผลลัพธ์ที่ worker สร้างไฟล์ส่งต่อเป็น file reference พร้อมชื่อ/ชนิด/ขนาดเท่านั้น งานที่ล้มเหลว/หยุด/ไม่สมบูรณ์ retry ต่อใน worker session เดิมผ่าน `follow_up_subagent` ได้ และงานใหม่สั่งต่อเข้า worker session เดิมผ่าน `continue_subagent` โดยคงประวัติเดิม (CHANGE-022)

REQ-020 — การ delegation ต้องจอง running job ได้พร้อมกันไม่เกินหนึ่งรายการต่อ parent session โดยครอบคลุม investigation, planned work, follow-up และ continue Job ต้องเก็บ plan revision และ step identity การ completion, retry, continue และ acceptance ต้องตรวจสอบ transition และห้ามเปลี่ยนแผนใหม่ที่เข้ามาแทนที่ History, stop, follow-up, continue และ acceptance ต้องตรวจสอบ ownership ของ parent History ของแต่ละ job ต้องแสดงรายการ tool ที่ worker ใช้พร้อมชื่อ, arguments/รายละเอียด และผลลัพธ์ (รวมสถานะ error) ของแต่ละ tool นอกเหนือจากข้อความสรุปและผลลัพธ์สุดท้าย Continue ต้อง reuse worker session เดิม (workerID/session database เดิม) สำหรับงานใหม่ได้แม้ job ก่อนหน้าจะถูก accept แล้ว โดยต้องไม่มี running job ซ้อนกันและต้องผูกกับ plan step ปัจจุบันหรือเป็น investigation เมื่อแผนเสร็จแล้วต้องยังสามารถทำ investigation หรืองานต่อเนื่องสำหรับงานใหม่ได้โดยไม่ล้างหรือข้ามงานที่กำลังทำอยู่ `stop_subagent` ต้องเป็น blocking call: ยกเลิก job แล้วรอจน terminal state จริง จึงคืนรายงานสุดท้าย (สถานะ stopped + ผลลัพธ์/ความคืบหน้าที่มี) กลับเป็น tool result ห้ามตอบ "ขอหยุดแล้ว" โดยแจ้งผลภายหลัง และการ cancel ของ parent turn ระหว่างรอต้องหยุด worker และคืนรายงานสถานะ stopped (CHANGE-022)

REQ-021 — (แก้ไขด้วย CHANGE-025) Continuation turn ที่ฉีดเข้า parent มีได้สองชนิดเท่านั้น: progress report (ทุก X tool calls) และ final report เมื่อ job จบ (ห้ามมี event ชนิดอื่นเช่น tool started/running); delegation call คืนเฉพาะ ack พร้อม job id; trace ของ worker ยังต้อง display ผ่านเส้นทางเดิมโดยรักษา routing identity (`channel_id` metadata) ของ input ที่เริ่มต้น turn ไว้เหมือนเดิม reservation/revision ของ orchestration เป็นแบบ process-local และการ persist session/cancellation behavior เดิมต้องไม่เปลี่ยน (CHANGE-022)

REQ-022 — Discord ต้องแสดง canonical response text เป็น Markdown ที่อ่านง่าย ไม่ใช่ SDK output ที่ serialize เป็นข้อมูลดิบ Progress ต้องกระชับ และไม่แสดง raw tool arguments/results, delegated task text, reasoning text หรือ internal planning/review chatter แต่ยังคงแสดง operation label ที่ปลอดภัย, ตัวบ่งชี้ความล้มเหลว, เวลา retry, usage และเวลาที่ใช้ได้อย่างมีประโยชน์ การ render ของ transport ต้องอยู่ใน Discord module ข้อความ progress/response ทั้งหมดของ transport ต้อง render แบบ Components V2 (Container + TextDisplay พร้อม `MessageFlagsIsComponentsV2` ทุก send/edit) ห้ามสร้าง embed ใหม่ (CHANGE-022)

REQ-023 — Discord final และ streamed response text ต้องแบ่งหน้าแบบ lossless ที่ขอบเขต Unicode code point ภายในข้อจำกัด message/embed โดยนับ supplementary characters เป็น UTF-16 units อย่างระมัดระวัง ต้องคง whitespace ที่ขอบเขตไว้ และเมื่อมี fenced code block ต้องปิด/เปิด fence สำหรับการแสดงแต่ละหน้าโดยไม่ลบ content ต้นฉบับ Streaming update ต้องแก้ไขหน้าที่มีอยู่ และ terminal response trace ต้องไม่ replay content ที่ถูก stream ไปแล้ว Progress สามารถเป็น summary ที่มีขอบเขตได้ แต่ response text ต้องไม่ถูกตัดทอน

REQ-024 — Discord text/progress transition ต้องส่งต่อ flush failure และเก็บ buffered state ที่ยังไม่ได้ส่งไว้เพื่อ retry แทนการ reset เมื่อส่ง page สำเร็จต้องบันทึกไว้เพื่อไม่ให้ retry ส่งซ้ำ Success/error terminal path ต้องหยุด live footer update, พยายาม flush buffer ที่ค้าง และล้าง retry-status แม้ operation อื่นจะล้มเหลว และต้องแสดง display error ให้ผู้ใช้เห็น โดยยังคงใช้ trace throttling/cooldown เดิม และ reset/close ต้องยกเลิก timer ที่รออยู่ คำสั่ง interaction ที่ต้องทำงานที่ใช้เวลา (สร้าง channel, โหลด model catalogue, บันทึก settings, อ่านรายการ session) ต้อง defer interaction response แบบ ephemeral ก่อนเริ่มงาน แล้วส่งผลลัพธ์หรือข้อผิดพลาดทาง followup เพื่อไม่ให้ interaction หมดอายุภายใน 3 วินาที ข้อยกเว้นคือการเปิด modal ซึ่งต้องตอบทันทีเพราะ Discord ไม่อนุญาตให้เปิด modal ต่อจาก deferred response

REQ-025 — Transport ที่รับไฟล์ (Discord attachment) ต้องดาวน์โหลดไฟล์นั้นแล้วแปลงเป็น file reference ใน file store และแนบ reference ผ่าน `Input.Metadata` โดยไม่เปลี่ยน canonical Turn/ContentPart contract และต้องคง session routing identity กับ metadata เดิมทั้งหมดไว้ รวมถึง `channel_id` ของต้นทาง ข้อความที่มีเฉพาะ attachment ต้องไม่กลายเป็น turn ว่าง — **ยกเลิก/แทนที่: inbound attachment boundary ผ่าน Discord ถูกถอด (CHANGE-059)**

REQ-026 — Worker agent ต้องมี tool สำหรับอ่านไฟล์จาก file store ภายใน root ที่จำกัดด้วย safePath/withinRoot discipline ของ module tool นั้น แล้วสรุปเนื้อหาและสถานะกลับให้ planner ส่วนการส่งไฟล์ออกแบบจริงต้องอยู่ใน transport module เท่านั้น และห้ามส่ง raw bytes หรือ base64 ผ่าน canonical SDK text path — **ยกเลิก/แทนที่: worker attachment tools ยังอยู่ แต่ inbound boundary เดิมถูกถอด (CHANGE-059)**

REQ-027 — Discord ต้องมีคำสั่ง `/new` สำหรับสร้าง text channel ใหม่ใน guild เดียวกับช่องที่เรียกคำสั่ง โดยตั้งชื่อช่องจากวันที่และเวลา (รูปแบบ `ai-YYYY-MM-DD-HHMM`) และคัดลอก model settings ของ session ต้นทาง (provider, model, temperature, thinking level, API key pool index) ไปยัง session ของช่องใหม่ทั้งหมด เมื่อสร้างสำเร็จต้องส่งข้อความสรุปการตั้งค่า (provider/model/thinking/temperature/API pool) เข้าไปในช่องใหม่ กรณีเรียกนอก guild, session ต้นทางยังไม่มี model settings หรือสร้างช่องไม่สำเร็จ ต้องตอบกลับแบบ ephemeral ในช่องเดิมโดยไม่สร้าง session ใหม่ พฤติกรรมทั้งหมดอยู่ใน Discord module และห้ามเปลี่ยน core orchestration, persistence หรือ canonical contract

REQ-028 — Discord คำสั่ง `/model` ต้องเปิด control panel แบบ Components V2 ในข้อความปกติข้อความเดียว (regular message ไม่ใช่ ephemeral เพราะแก้ไขไม่ได้) แล้วแก้ไขข้อความเดิมทุกครั้งที่มีการกด โดยทุก control บันทึกผลทันที ไม่มี staging ข้อความประกอบด้วย Container กล่องเดียว (accent bar เดียวกันทั้งกล่อง: หัวข้อ ค่า settings แยกฝั่ง Main/Sub แบบ plain text ไม่มีอิโมจิ แล้วตามด้วย controls ทั้งหมดในกล่อง ทั้งปุ่มและ select menu ผ่าน ActionRow) ได้แก่แถวปุ่ม Main/Sub ต่อด้วย select menu provider (ไม่ preselect), select menu model (ไม่ preselect), แถวปุ่ม Thinking/Temp, select menu API pool และปุ่ม Save สีเขียวล่างสุดซึ่งกดแล้วถอด controls ทั้งหมดออกเหลือสรุปอย่างเดียว (freeze) ปุ่ม Thinking/Temp ใน container กดแล้วเปิด modal ให้เลือก thinking จาก select menu และกรอก temperature เป็นตัวอักษร (default หรือ 0.0–2.0) เมื่อ submit ต้องบันทึกแล้วแก้ไข panel ข้อความเดิม มีเส้นคั่นระหว่างฝั่ง Main/Sub อิโมจิมีเฉพาะในปุ่มและเมนูเท่านั้น สรุปเป็น plain text เมื่อ provider หรือ model catalogue มีรายการเกิน 25 ให้แบ่งหน้าในตัวเลือกเองโดย `Next` อยู่ด้านบนเสมอ (หน้าแรก `Next` + 24 รายการ หน้ากลาง `Previous` + `Next` + 23 รายการ หน้าสุดท้าย `Previous` + รายการที่เหลือ) และชื่อเมนูต้องบอกหน้าปัจจุบัน (เช่น `Select model (1/2)`) select menu ทุกเมนู (provider/model/API pool) ไม่ preselect ค่าปัจจุบันดูในสรุป การเปลี่ยน provider ต้องคง model เดิมไว้ถ้ายังอยู่ใน catalogue ไม่เช่นนั้นใช้ model แรกของ catalogue เพื่อให้ session ใช้งานได้เสมอ controls ทุกตัว (รวม modal thinking/temperature) อ่านและบันทึก settings ของฝั่ง active เท่านั้น การสลับโหมดต้องรีเซ็ตหน้า model มาที่ model ของฝั่งนั้น

REQ-029 — Session ต้องจำ agent mode (`main` หรือ `sub`, ค่าเริ่มต้น `main`) แบบ persist โหมด `main` ทำงานแบบ planner (planning prompt + orchestration tools + read-only context tools; ไม่มี write/exec tools) (CHANGE-054) โหมด `sub` ทำงานแบบ worker ตรง (execution tools เต็ม ไม่มี planning wrapper) โดยไม่ต้องพึ่ง global `DisablePlanning` switch การสลับโหมดต้องมีผลกับ turn ถัดไปของ session นั้นทันที

REQ-030 — แต่ละ Discord channel มีสอง persistent sessions แยก db กัน: main (`discord:channel:<id>`) ทำงานแบบ planner, sub (`discord:channel:<id>:sub`) ทำงานแบบ worker โดยแต่ละ session เก็บ settings ของตัวเอง (provider, model, thinking level, temperature, API key pool index) ใน top-level fields ไม่มี overlay gateway ต้อง route ข้อความของ channel ไป session ตามโหมด active ที่จำไว้บน main session (default main) sub session ต้อง reuse ได้ตลอดจนกว่าจะลบ การเข้า sub ครั้งแรกที่ยังไม่มีค่าต้อง seed จากฝั่ง main `/new` ต้องคัดลอกทั้งสอง sessions พร้อมโหมด

REQ-031 — Discord progress trace ของ Main Agent และ Sub-agent render เป็น Components V2 โดยแต่ละ actor ได้ข้อความของตัวเองพร้อม accent สีแยกกัน (planner blurple, worker เขียว) ภายใน actor แก้ข้อความเดิมแบบ in-place เสมอ ห้ามใช้ embed ใหม่ ลำดับข้าม actor ต้องถูกต้องตามเวลาจริง: transport ต้องนับลำดับข้อความที่ created ต่อ channel ถ้ามีข้อความใด (ของ actor ไหน หรือข้อความ response text) ถูกสร้างต่อหลังข้อความปัจจุบันของ actor แล้ว บรรทัดใหม่ของ actor นั้นต้องเปิดข้อความ segment ใหม่ด้านล่าง ห้าม append บรรทัดใหม่ลงในข้อความที่เก่ากว่าข้อความล่าสุด และเมื่อจบ turn ต้องปิดข้อความเดิม (completed) ข้อความถัดไปเริ่มใหม่ (CHANGE-023); main panel เป็น unified ข้อความเดียวต่อ actor โดย response content ต่อท้ายใน items เดียวกันแบบ top-truncation แทนการส่งข้อความแยก ส่วน sub คง tool-only (CHANGE-051) — **ยกเลิก/แทนที่: Discord actor display ถูกถอด (CHANGE-059)**

REQ-032 — Trace ต้องแสดง tool ทุกตัวที่เรียกจริงใน turn นั้น ไม่ซ่อน orchestration tool ใด (`plan`, `stop_subagent`, `accept_subagent_result`, `delegate_to_subagent` ฯลฯ ต้องเห็นเป็นบรรทัด) แต่ละบรรทัด tool ต้องแสดงชื่อ, สถานะ, เวลา และ arguments ของ tool แบบ one-line ที่ตัดทอนความยาวปลอดภัย (ค่า argument เป็นสิ่งที่ transport ยอมรับให้แสดงได้ตั้งแต่ CHANGE-023) ส่วน raw result ของ tool, delegated worker task text, reasoning text และ internal chatter ห้ามแสดง และการดำเนินของ worker ระหว่าง delegation ต้องเห็นใน trace เรียงตามเวลาที่ emit จริง (CHANGE-023)

REQ-035 — Execution tool ของ shell ต้องชื่อ `bash` และรับ shell command line ตรง ๆ เป็นสตริงเดียว (ไม่มี args array แยก, ไม่มีข้อจำกัด multi-word) รันผ่าน bash ใน workspace พร้อม timeout และ process-tree kill like เดิมของ `run_command`; คำสั่ง long-running เดิม (`run_job`/`check_job`/`close_job`) คง behaviour เดิม (CHANGE-024) — **ยกเลิก/แทนที่: Discord progress/typing ถูกถอด (CHANGE-059)**

REQ-036 — Role ของ Main Agent คือ planner ไม่ใช่ผู้ส่งต่อ: ห้ามส่งข้อความดิบของผู้ใช้เป็น task ของ worker ตรง ๆ — ต้องแตกงานเป็นขั้น, เขียน task เป็นภาษาอังกฤษในรูป delegation contract (Objective, Non-goals, Authority — allowed paths/commands/forbidden, Expected tests, Required evidence, Acceptance criteria), และตรวจสอบ work package + evidence ก่อน accept ทุกครั้ง — trust ต้องมี evidence กำกับ (CHANGE-024, CHANGE-054) — **ยกเลิก/แทนที่: Discord pagination ถูกถอด (CHANGE-059)**

REQ-037 — เมล trace ที่ actor ใดมีบรรทัด "provider accepted" ค้างอยู่เมื่อ content มาถึง: ถ้า container นั้นมีเฉพาะบรรทัด accepted อย่างเดียว ให้แปลงข้อความนั้นเป็น response content โดยตรง (edit ในที่, ไม่สร้างข้อความใหม่) ถ้ามีบรรทัดอื่นอยู่ด้วย ให้ลบบรรทัด accepted ออกจาก container แล้วส่ง content เป็นข้อความใหม่ตามลำดับ (CHANGE-024)

REQ-033 — ตัวจับเวลาใน trace ต้องวัดจากช่วง `sending request to provider` จน tool execute สำเร็จจริง ด้วยการลบกันของ Unix millisecond timestamp ที่บันทึกไว้ ณ แต่ละ event (แบบเดียวกับ turn footer) ไม่ใช่ `time.Since` ณ ตอนรับ event ฝั่ง display; บรรทัดที่ยัง exec ไม่จบแสดงเวลาสะสมตั้งแต่ request นั้น และ tool ที่เสร็จแล้วแสดงเวลาตั้งแต่ส่ง request จน exec สำเร็จ; ตัวบ่งชี้ "provider accepted" ต้องรวมอยู่ในกลุ่ม display ของ request/tool เดียวกัน ห้ามเป็นบรรทัดอิสระที่ค้างอยู่เมื่อ tool เริ่มทำงาน (CHANGE-022)

REQ-034 — Plan ต้องใช้งานได้จริงเป็น checklist ของ Main Agent: (1) checklist พร้อมสถานะของแต่ละ step ต้องถูก inject เข้า system prompt ของ planner ทุก iteration เพื่อให้ Main มองภาพรวมโปรเจคตลอดโดยไม่อ่าน history เอง (2) sub-agent ทำงานย่อยทีละ step ตามแผน (3) delegation คืน job id ทันที และ Main ได้รับรายงานเองโดยอัตโนมัติ — progress ทุก X tool calls และ final report ครบ (terminal status, final result, validation evidence, สรุปของ worker) ตอนงานจบ ไม่มี tool polling status/history (ลด API call) (4) `stop_subagent` เป็น blocking เช่นกัน: รอจน worker หยุดจริงแล้วคืนรายงานสุดท้าย (stopped + ความคืบหน้า/tool history) ในผลตอบเดียว ห้าม "สั่งหยุดแล้วแจ้งผลทีหลัง" (5) เครื่องมือ orchestration ที่เหลือ (`accept_subagent_result`, `follow_up_subagent`, `continue_subagent`, `plan`) คง semantics เดิม (CHANGE-022)

REQ-038 — Discord ต้องมีคำสั่ง `/workspace <path>` สำหรับเลือกตำแหน่งทำงานของ channel นั้น (appplied กับทั้งสอง sessions main+sub) โดยรองรับ `~/` และ `~` (expand เป็น home directory ของ user ที่รัน daemon), ต้องเป็น path ที่มีอยู่จริงและเป็น directory เท่านั้น (ไม่สร้างใหม่), บันทึกแบบ persist ใน session (คอลัมน์ `workspace`) และมีผลกับ tool ทุกตัวที่resolve root ต่อ invocation ตาม session ของ turn นั้น (main turn, sub-mode turn, และ worker ที่ delegate ออก) — session ที่ไม่มีค่าใช้ global workspace เดิม; เรียกโดยไม่ใส่ path ต้องแสดงค่าปัจจุบันพร้อม status; ผลตอบเป็น ephemeral ใน channel; `/new` ต้องคัดลอก workspace ไป channel ใหม่ด้วย (CHANGE-025)

REQ-039 — (แก้ไขด้วย CHANGE-030) Adapter `opencode` ต้องยิง `/responses` ก่อนแล้วตกไป `/chat/completions` เมื่อ endpoint นั้น serve โมเดลไม่ได้ (404, 400 `model_not_supported_on_endpoint`, หรือ 500 — ตรวจ live แล้วว่า Zen แยกโมเดลตาม endpoint ทั้งสองทิศ: spark ใช้ได้เฉพาะ `/responses`, mimo ใช้ได้เฉพาะ `/chat`) โดย 401/403/429 ต้อง return ทันทีไม่ fallback; ทุก request พร้อม fingerprint ของ opencode ตัวจริงที่ตรวจจาก binary บนเครื่อง (v1.18.31): `User-Agent: opencode/<version>` (ค่าเริ่มต้นคือเวอร์ชันที่ตรวจพบ), `HTTP-Referer: https://opencode.ai/`, `X-Title: opencode` และ `x-opencode-session: <ses_-id>`; ses_-id ทรง `ses_f<hex 8 ตัว>ffe<สุ่ม 14 ตัวอักษร A-Za-z0-9>` โดย hex ลดตามเวลาขึ้น (time-descending แบบ opencode) ถูก mint ครั้งเดียวต่อ harness session แล้ว reuse ตลอด process ผ่าน cache ใน memory; custom headers จาก provider config ชนะค่าเริ่มต้นของ UA/Referer/Title ได้แต่ชนะ session header ไม่ได้ (session header เป็นของ adapter เสมอ); body ต้องไม่มี `user` field (ตรงกับ opencode ที่ส่ง session ผ่าน header เท่านั้น); BaseURL ค่าเริ่มต้น `https://opencode.ai/zen/v1`; ListModels ใช้ `GET /models` จริง (คง REQ-007); เลือก adapter ผ่าน `"adapter": "opencode"` แบบ explicit เท่านั้น ไม่เปลี่ยน name inference เดิม (`opencode` ยัง infer เป็น anthropic); RouterClient ต้อง stamp `Request.SessionID` จาก session ทุกครั้งก่อนส่งให้ provider (CHANGE-026) — **ยกเลิก/แทนที่: Discord-adapter header behaviour ถูกถอด (CHANGE-059)**

REQ-040 — Provider HTTP ทุกเส้น (DoJSON/SSE ส่วนกลาง) ต้องส่ง `User-Agent` เริ่มต้น (`ai`) เมื่อ adapter ไม่ได้กำหนดมาเอง — gateway บางราย (เช่น NousResearch ผ่าน Cloudflare) ปฏิเสธ `Go-http-client` ด้วย 403 HTML ทำให้ discovery ล้มเหลว; header ที่ adapter/config ระบุไว้ชัดต้องชนะค่าเริ่มต้นเสมอ (คง REQ-039 ที่ UA ของ adapter opencode ยังมีผลเหนือ) และการตอบ interaction ของ Discord ที่รายงาน provider error ต้องถูกตัดให้สั้นกว่าขีดจำกัด 2000 ตัวอักษรของ Discord เสมอ (log ฝั่ง daemon เก็บข้อความเต็ม) มิฉะนั้นผู้ใช้จะไม่เห็นสาเหตุจริง (CHANGE-027) — **ยกเลิก/แทนที่: รวม Discord error truncation แล้วถูกถอด (CHANGE-059)**

REQ-041 — (แก้ไขด้วย CHANGE-056) Discord แสดง progress ผ่าน actor panels ของ REQ-031 เท่านั้น — ไม่มีข้อความสถานะแยก (heartbeat status box และ receipt ถูกถอดออกเพราะแสดง tool lines/usage ซ้ำกับ panels): แต่ละ actor (main/worker) มี panel ของตัวเองที่แสดง tool calls พร้อม args excerpt, response content (เฉพาะ main) และ usage footer สองบรรทัด (turn/session) แบบ in-place edit; ระหว่าง tool events transport ส่ง Channel typing indicator อย่างเดียว (throttle ไม่เกิน 1 ครั้งต่อ 3 วินาที) ห้าม ticker รายวินาที ห้ามส่ง progress message ใหม่; คง V2 path, pagination, accent colors, routing เดิม (CHANGE-036, CHANGE-056) — **ยกเลิก/แทนที่: Discord actor panels ถูกถอด (CHANGE-059)**
REQ-042 — Worker agent ต้องมี OS control suite ครบ 10 tools (`os_mouse_move`, `os_mouse_click`, `os_key_press`, `os_type_text`, `os_screenshot`, `os_mouse_drag`, `os_mouse_scroll`, `os_window_list`, `os_window_focus`, `os_window_geometry`) ผ่าน xdotool บน X11 โดยมี wtype เป็น fallback สำหรับ keyboard/text บน Wayland; mouse/window tools ต้องคืน error ที่ชัดเจนภายใต้ wtype backend (wtype รองรับ keyboard เท่านั้น); `os_screenshot` เก็บ PNG ใน session attachment store เป็น file reference เท่านั้น (name/content_type/size/ref_id/path ไม่ส่ง bytes/base64 ผ่าน text path) รองรับ display/name/scale advisory พร้อม resize note; `os_mouse_drag` ลากพร้อมกดปุ่มผ่าน mousedown/mousemove/mouseup chain พร้อม steps; window tools ผ่าน xdotool search/windowactivate/getwindowgeometry; ต้อง validate arguments (พิกัด clamp 0–16384, key ยาวไม่เกิน 64, text ไม่เกิน 4000 runes, drag steps 1–50, scroll amount 1–20, pattern/window-id/display/name limits) และรองรับ dry-run ผ่าน `AI_OS_INPUT_DRY_RUN=1` โดยไม่แตะ display server; เมื่อ `DISPLAY` ว่างแต่มี socket `:1` (`/tmp/.X11-unix/X1`) xdotool paths ต้อง fallback ใช้ `DISPLAY=:1` อัตโนมัติ (explicit display param ของ `os_screenshot` ยังชนะเสมอ, wtype fallback และ validation/dry-run คงเดิม); browser tools เดิมคงอยู่ครบ ไม่ถูกแทนที่หรือลบออก (CHANGE-045, CHANGE-046, CHANGE-047)

REQ-043 — `ai update [--auto] [version]` ต้อง self-update แบบ blue-green เท่านั้น (single flow — ไม่มี legacy replace-and-start branch: ถ้า daemon ไม่รัน ให้ start daemon ก่อนแล้วทำ blue-green handover ตามปกติ) พร้อม job handoff ไป daemon ใหม่โดยไม่มีช่วง zero-daemon; binary ตัวเดียว (`ai`) เป็นเจ้าของ handover end-to-end ห้ามมี supervisor ภายนอก (scripts/keepalive.sh และ scripts/supervisor.sh ถูกถอดออก) (CHANGE-055): ดาวน์โหลด binary ที่ verify hash แล้ว (คงพฤติกรรมเดิม: verify checksum ก่อน, ไม่ restart ถ้า hash ไม่เปลี่ยน), drain งาน in-flight ขณะ blue ยัง serve (รอ background jobs ใน `data/jobs.json` ด้วย timeout), stage binary ใหม่ไว้ temp path แล้ว start green daemon ใน standby/probation mode (`daemon --standby`, ไม่ connect Discord intake, ใช้ shadow pid `ai.pid.green` + shadow heartbeat `discord.heartbeat.green` + phase file `update.bluegreen.json`) โดยยังไม่หยุด blue, health gates ทั้งหมดต้องผ่านภายใน 60–90s (single-binary ownership: updater ทำ stage/standby/gates/stop/promote/confirm/rollback เองทั้งหมด) (green pid alive ผ่าน kill-0, log มี ready marker ที่ correlate กับ green pid นั้น (ห้ามใช้ marker ค้างจาก handover ก่อน — CHANGE-057), shadow heartbeat fresh ภายใน 60s, handoff ถูก consume) จึง SIGTERM blue ด้วย drain 30s เดิมแล้ว promote green เป็น `ai.pid` แบบ atomic พร้อม enable live intake, green ล้มเหลวต้อง rollback (kill green, ลบ shadow/phase/staged เก็บ blue/binary เดิมไว้ พร้อม error และ log tail) ห้ามเหลือ zero-daemon window; ถ้า promote ล้มเหลวหลัง blue หยุดแล้ว ต้อง emergency live-promote green ที่ staged (rename + mark cutover + confirm live) ห้ามจบแบบ zero-daemon เงียบ ๆ; standby หมดอายุต้องล้าง phase + handoff ด้วย (ห้ามเหลือ phase orphan); cutover มีเจ้าของเชิงตรรกะเดียว (updater ทำ atomic file ops ส่วน standby re-assert ค่าเดียวกันแบบ idempotent ไม่ใช่ผู้ตัดสินใจคนที่สอง); jobs ที่ยัง running ข้าม restart ต้องคง command/args/session/output ไว้และถูก mark เป็น `interrupted` (retryable) แทน `failed`, intake กลับมา resume จาก live transports บน daemon ใหม่, เขียน handoff file (`update.handoff.json` มี old/new version+hash, old pid, drain flag) เพื่อให้ daemon ใหม่ log resume บน boot แล้วลบ, promote แล้วต้อง verify live intake (live `ai.pid` + live marker ที่ correlate กับ green pid นี้ + live heartbeat fresh) ก่อนจบคำสั่ง (CHANGE-049, CHANGE-053, CHANGE-055, CHANGE-057) — **ยกเลิก/แทนที่: `ai update` blue-green ถูกถอดทั้ง feature (CHANGE-059)**

REQ-044 — Discord gateway ต้องติดตาม liveness ของ websocket (handlers Ready/Disconnect/Resumed บวก last-event timestamp ทุก MessageCreate/InteractionCreate, Connected() รายงาน flag), watchdog goroutine รีเฟรชไฟล์ `discord.heartbeat` ใต้ state root ขณะ connected และ reopen session ด้วย backoff (5 ครั้ง, เริ่ม 1s ทวีคูณ) เมื่อเงียบนานเกิน 3 นาทีหรือ flag หลุด; `discord.heartbeat` เก็บไว้เป็น operator-visible liveness signal (ไฟล์ยังไม่มีก่อน Ready ครั้งแรกไม่นับว่าล้มเหลว); automated repair จบที่ in-process watchdog reopen — ไม่มี supervisor ภายนอก: daemon ตายหรือ gateway กู้ไม่ขึ้นต้อง `ai start` เอง (tradeoff ที่ยอมรับใน CHANGE-055); ไม่แตะ V2 display, OS tools, หรือ update path (CHANGE-050, CHANGE-055) — **ยกเลิก/แทนที่: Discord gateway liveness/heartbeat ถูกถอด (CHANGE-059)**

REQ-045 — Dual loop-control: system caps บวก prompt discipline พร้อม checklist, tool budgets และ failure lessons — ระบบบังคับ hard caps ในโค้ดสำหรับ worker loop: max tool calls ต่อ turn attempt, max consecutive read/edit calls ที่ไม่มี progress (read_file/read_files/list_directory/search_files/edit_file ติดกันโดยไม่มี progress tool คั่น), max bash output size ต่อ result; เมื่อเกินต้อง auto-fail ทันทีด้วย error ที่ชัดเจนและห้าม retry ซ้ำ (budget failure เป็น fatal); คงพฤติกรรมเดิมทั้งหมดเมื่ออยู่ใต้ limits (turn ปกติที่ใช้ single-digit calls ไม่เปลี่ยน) — ส่วน prompt discipline: checklist ตามลำดับต่อ task ใน `requirements/loop-control.md`, ทุก delegation ต้องประกาศ tool budget (max reads/edits/bash), batch reads ผ่าน `read_files`, stop condition เมื่อ budget เกิน = fail แล้วเขียน lesson ลง `requirements/lessons.md` แทนการวนซ้ำ; Main ใช้ read tools เองภายใต้ budget อ่านของตัวเอง (index.md ก่อน แล้ว batch read_files — ดู planner self-read ใน requirements/loop-control.md) (CHANGE-054); planner/worker prompts อ้างอิง checklist นี้; ไม่แตะ transport/discord display, gateway liveness, keepalive, OS tools, update path (CHANGE-052) — **ยกเลิก/แทนที่: loop-control ยังใช้; เฉพาะส่วน Discord-display ถูกถอด (CHANGE-059)**

REQ-046 — Mobile transport + stateless runtime state (CHANGE-058): (1) `transport/mobile` เป็น WebSocket input source/display ฝั่ง daemon ที่พูด JSON ตรงกับ `sdk.Input`/`sdk.Output` และวางผ่าน `sdk.MergeInputSources` + `sdk.DispatchDisplay` เหมือน transport อื่น โดยไม่แตะ core loop; (2) การเข้าถึง mobile ใช้ 2 ขั้น: hostname ของ Cloudflare quick tunnel (สุ่ม ยังไม่ publish อะไร) + header `Authorization: Bearer <Cloudflare API token>` ตรวจกับ `GET /user/tokens/verify` ก่อน upgrade และเก็บ token ที่ผ่านไว้ใน RAM — การเชื่อมต่อครั้งถัดไปที่ใช้ token เดิมผ่านทันทีโดยไม่ต้องยิง Cloudflare ซ้ำ (token ผิดยังโดน 401 + lockout เหมือนเดิม, และ handshake ที่ถูกปฏิเสธต้องถูก log เป็น address + เหตุผลโดยไม่เขียน token) — header ขาด/ผิดรูปแบบ = `401` และไม่นับ, token ผิด 5 ครั้ง = `429` ล็อก address นั้น 30 วิ แล้ว 60/120/240/300 วิ (เพดาน) โดยนับตาม `CF-Connecting-IP` → `X-Forwarded-For` → peer (หลัง tunnel peer เป็น cloudflared local เสมอ) และห้ามใช้ token เป็นส่วนของ key (หมุน token หลุดล็อกได้), success รีเป็น 0, Cloudflare ติดต่อไม่ได้ = `503` ไม่นับ (fail closed); (3) daemon ไม่มี credential ของตัวเอง — เมื่อ handshake ผ่านจึงเก็บ Cloudflare token ใน memory เท่านั้น (restart หาย แล้วรอมือถือส่งใหม่) และใช้ token นั้นยิง Cloudflare API ต่อ; account id และ database id ต้องถูกหาจาก token เอง (`GET /accounts` → `GET /accounts/{id}/d1/database`) ไม่ต้องตั้งค่า id ใน `config/entry.json`; (4) `runtime/d1store` ทำ D1 เป็นต้นทางของ runtime state: `config/provider|system|attachment|browser` (ไม่รวม `config/entry.json` เพราะเป็น bootstrap ของ gateway ที่ต้องอ่านก่อนถึง D1 ได้) และไฟล์ session DB ถูก materialize ลง `config/*.json` + `data/sessions/*.db` ตอน handshake แรกของ connection ใหม่ (local files ยังเป็นรูปแบบที่ loader/saver เดิมใช้ จึงไม่เปลี่ยน CON-001/CON-002) และถูก push กลับหลังเปลี่ยน (ขนาดเกิน limit ของ D1 = ข้ามพร้อม log ไม่ล้มทั้ง runtime); (5) ทุก turn ที่จบถูกเขียนเข้า D1 ผ่าน `POST /accounts/{a}/d1/database/{d}/query` ตามลำดับ FIFO เพื่อให้ประวัติบนมือถือเรียงตาม `seq`; หลัง hydrate config สำเร็จ daemon ต้อง reload provider (adapters/router) จาก config ที่เพิ่งถูกดึง และ `transport/mobile` ต้องแสดงคำตอบของ turn ที่ถูก trace โดยอ่าน text จาก `TraceResponseContent` ปิด turn ที่ `TraceResponse` (turn ที่ trace แล้วไม่มี final `output.Content`) — user turn ที่รับเข้าต้องถูกเขียนลง D1 ด้วย ไม่ใช่เฉพาะ model turn; session ที่ materialize มาจาก D1 ต้องถูกย้ายไปใช้ provider ที่ daemon มีอยู่จริงหลัง reload (ถ้า provider เดิมไม่มีแล้ว) ไม่เช่นนั้นทุก turn ของแชทเก่าจะล้มด้วย `sdk: provider is required`; (6) ไม่เพิ่ม credential ใหม่ (ไม่มี node token, ไม่มี state encryption key) — config/provider ที่เก็บใน D1 อ่านได้ด้วย Cloudflare token เดียว (single-operator tradeoff, CON-012); daemon ต้องเสิร์จ `/api/sessions*`, `/api/turns*`, `/api/models`, `/api/node`, `/api/ping` เองจาก D1 ผ่าน tunnel และต้อง stream ทุก turn: frame `delta` ต่อชิ้นข้อความ, `trace` สำหรับความคืบหน้า/เครื่องมือ, `message` เฉพาะตอนที่คำตอบไม่ได้มาเป็น delta และ `done` ปิด turn; มือถือเลือก provider/model ต่อแชทได้ (`GET /api/models` + `PATCH /api/sessions/:id`) โดย daemon ตรวจกับ router, ใส่ให้ session ที่เปิดอยู่ และเก็บลง D1 เพื่อให้ค่าอยู่ข้ามการรีสตาร์ต; adapter ต้องเลือก streaming dialect ตาม endpoint (gateway แบบ OpenAI-compatible ใช้ `/chat/completions`) และ D1 ต้องได้หนึ่ง turn ต่อหนึ่ง turn บนสาย เพื่อให้แอปใช้ที่อยู่เดียว และ `/api/state` ต้องไม่ถูกเสิร์จเด็ดขาด (กัน provider keys หลุดออกทาง tunnel)

REQ-047 — Daemon มี gateway เดียว: `transport/mobile` ผ่าน Cloudflare quick tunnel (CHANGE-059) — `ai` เป็น daemon สำหรับ AIxodia เท่านั้น ไม่มี CLI transport, ไม่มี Discord transport, ไม่มีคำสั่ง `ai update`/`uninstall`/`stop`/`discord`/`browser`/`system`/`cli`/`provider` และไม่มี self-update/blue-green/pid-handoff; จุดเข้าถึงเดียวคือ WebSocket บน tunnel URL ที่ค้นหาได้จาก `GET /api/node` และต้องผ่าน handshake 2 ขั้นตาม REQ-046; runtime config/session state อยู่ที่ Cloudflare D1 (REQ-046) จึงไม่มีคำสั่งจัดการ config แบบ local — การตั้ง provider/model/system prompt ทำผ่าน D1 (`/api/state/...`) ไม่ใช่คำสั่งใน binary; process เดียวรับ `daemon` (หรือรันเปล่า) แล้วทำงานจนถูก signal

REQ-048 — มือถือเป็นเจ้าของ provider, key pool และโมเดลของแต่ละ agent (CHANGE-066): (1) `transport/mobile/admin.go` เสิร์จผ่าน tunnel (หลังผ่าน gate ตาม REQ-046) เฉพาะ `GET|POST /api/providers`, `POST /api/providers/{id}/keys` (add/remove/replace), `DELETE /api/providers/{id}`, `POST /api/providers/refresh` และ `GET|PUT /api/settings`; provider ใหม่ต้องผ่าน router validation ก่อนถูกยอมรับ และการ probe ของ `refresh` ต้องยิงจริง 1 token เพื่อให้เห็นเหตุผลจริง (เช่น 401 key ตาย, 403 free tier) ไม่ใช่แค่ 200 จากการบันทึก; (2) key เป็น write-only — ไม่มีเส้นทางไหนส่งค่า key กลับออก tunnel (รวมทั้ง error message), ตอบกลับเป็นจำนวน key + ข้อความสถานะ/เหตุผลเท่านั้น; (3) การเขียน provider/settings ต้องเขียน `config/provider` + `config/system` ทั้งไฟล์และ D1 (REQ-046(4)) แล้ว reload runtime ทันที เพื่อให้ค่าของมือถืออยู่ข้าม restart โดยไม่ต้องแก้ไฟล์บนเครื่อง; (4) `PUT /api/settings` ตั้ง main/sub route (provider + model) ของ agent แต่ละตัวโดยตรวจกับ router และใช้แทนค่า boot; (5) มือถือหยุด turn ได้: เฟรม `cancel` ให้ daemon ยกเลิก context ของ turn ที่กำลังวิ่งใน session นั้นทันที (provider request + tool call) แล้วตอบ `done` stage `cancelled`; ถ้าไม่มี turn วิ่งอยู่ตอบ `done` stage `already_done` (ไม่ใช่ error) และ trace ที่ถูกยกเลิกต้องกลายเป็น `done/cancelled` ไม่ใช่ provider error; (6) turn ที่ล้มต้องรายงานกลับมือถือผ่านเฟรม `error` (ข้อความสั้น) เพื่อให้เห็นเหตุผลจริง เช่น `Invalid API key` หรือ `FreeTierError` แทนการ retry เงียบ ๆ; (7) สถานะ provider เป็นสามค่า: provider ที่ยังไม่เคย probe ต้องรายงาน `probed=false` และ `reachable` ที่ยังไม่รู้ห้ามถูกนับเป็น "ใช้ได้" — การแก้ key pool ของ provider ต้องล้างผล probe เดิมเพื่อให้รายงานว่ายังไม่ทดสอบอีกครั้ง; (8) ต้องทดสอบ provider เดี่ยวได้โดยไม่ต้องรอทั้งรายการ (`POST /api/providers/{id}/refresh` → `ProviderStatus`) เพราะผู้ใช้ที่กำลังแก้ key ที่ตายต้องการคำตอบของตัวนั้นก่อน
